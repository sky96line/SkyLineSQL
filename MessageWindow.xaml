<Window x:Class="SkyLineSQL.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkyLineSQL"
        mc:Ignorable="d"
        Title="MessageWindow" Height="150" Width="300"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        Background="Transparent" BorderBrush="Red" BorderThickness="1">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF333333" />
            <!-- Dark Background -->
            <Setter Property="Foreground" Value="#eee" />
            <!-- Border Thickness -->
            <Setter Property="Padding" Value="5,5,5,5" />
            <!-- Button Padding -->

            <Setter Property="BorderBrush" Value="LightCoral"/>
            <Setter Property="BorderThickness" Value="1"/>

            <!-- MouseOver State -->
            <Style.Triggers>

                <!-- Pressed State -->
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF666666" />
                    <Setter Property="Foreground" Value="#FFFFFFFF" />
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF666666" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Gray" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" x:Name="txt_title">Title</TextBlock>
        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Foreground="LightGray" FontSize="12" x:Name="txt_msg">Error: Something went wrong.</TextBlock>

        <Button Grid.Row="2" Grid.Column="1" Width="80" Height="30" HorizontalAlignment="Right" VerticalContentAlignment="Center" x:Name="btn_ok" Click="btn_ok_Click">Ok</Button>
    </Grid>
    
</Window>
